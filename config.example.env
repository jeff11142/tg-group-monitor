# ===== Telegram API 憑證 =====
# 到 https://my.telegram.org → API development tools 申請，會拿到這兩個值
API_ID=
API_HASH=

# 第一次登入用的手機號碼（含國碼，例如 +886912345678）
PHONE=

# session 檔名稱（登入後會產生 <SESSION_NAME>.session，之後不用再輸入驗證碼）
SESSION_NAME=tg_monitor

# ===== 要監聽的來源群組 =====
# 可填：群組的 @username、邀請連結後綴、或數字 ID（例如 -1001234567890）
# 不確定 ID 時，先把 LIST_DIALOGS=1 跑一次，會印出你所有對話的名稱與 ID
SOURCE_CHAT=

# ===== 關鍵字過濾（逗號分隔，不分大小寫）=====
# 訊息內容只要「包含任一關鍵字」就會被記錄與轉發；留空＝全部訊息都轉發
KEYWORDS=

# ===== 轉發目標：用 Bot 廣播給「接收者清單」=====
# 1. 到 TG 找 @BotFather → /newbot 取得 token，填這裡
# 2. 對你的 bot 按一次 /start（bot 才能私訊你），再把你的 chat_id 填到 BOT_TARGET
#    （取得 chat_id：找 @userinfobot 查你的數字 ID）
# 3. ADMIN_CHAT_ID 是「能對 bot 下管理指令的人」，通常就是你自己 (= BOT_TARGET)
#    指令清單：/list /add /remove /enable /disable /myid /help
# 4. BOT_TARGET 只在第一次跑時被用來自動建立接收者清單的第一筆；之後 DB 是 source of truth，
#    要新增/移除接收者請對 bot 用 /add /remove 指令（資料存在 recipients.db）
BOT_TOKEN=
BOT_TARGET=
ADMIN_CHAT_ID=

# ===== 訂閱服務（USDT 付款，管理員手動確認後開通）=====
# 用戶私訊 bot → 收到 chat_id 與下面的付款資訊 → 轉 USDT、把 TxID 給管理員
# → 管理員確認後對 bot 下 /sub <chat_id> <天數> 開通；到期自動停止推播並通知
# 非管理員私訊看到的方案文案
SUB_PRICE_USDT=30
SUB_DAYS=30
SUB_NETWORK=TRC20
SUB_WALLET=
# 訂閱到期檢查間隔（秒）
SUB_CHECK_INTERVAL=300

# ===== 轉發目標 2：Webhook / Discord / 自己後台 =====
# 任何接收 JSON POST 的 URL；Discord webhook 也支援。留空＝不發 webhook
WEBHOOK_URL=

# ===== 本機記錄 =====
# 是否把命中的訊息同時寫入本機檔案（JSON Lines）
LOG_TO_FILE=1
LOG_FILE=messages.jsonl

# 只列出對話清單後結束（找 SOURCE_CHAT 的 ID 用），設 1 啟用
LIST_DIALOGS=0

# ===== 幣安現貨自動交易（依訊號自動下單）=====
# 總開關：1 才會自動下單；0＝只監聽轉發不交易
TRADING_ENABLED=0
# ⚠️ 強烈建議先用 testnet（測試網假錢）驗證：1=testnet，0=正式網（真錢）
# 現貨 testnet 金鑰：https://testnet.binance.vision/
# 合約 testnet 金鑰：https://testnet.binancefuture.com/（與現貨是不同網站、不同金鑰）
# 測試/正式網切換：1=測試網假錢、0=正式網真錢。也可由 TG Bot /config 一鍵切換（會寫回此檔）
BINANCE_TESTNET=1
# 測試網金鑰（合約：testnet.binancefuture.com）
BINANCE_TESTNET_API_KEY=
BINANCE_TESTNET_API_SECRET=
# 正式網金鑰（真錢，需開合約權限）；要用正式網才需填
BINANCE_MAINNET_API_KEY=
BINANCE_MAINNET_API_SECRET=
# 合約模式（USDT-M 永續）：1=合約 0=現貨。合約金鑰需開啟「合約交易」權限
BINANCE_FUTURES=0
# 合約槓桿倍數（建議先用 1）、保證金模式（ISOLATED 逐倉 / CROSSED 全倉）
LEVERAGE=1
MARGIN_TYPE=ISOLATED
# 方向：LONG 做多（現貨只支援這個）；SHORT 做空（僅合約，目前訊號為多單先用 LONG）
TRADE_SIDE=LONG
# 止損保險絲（合約）：對帳時若價格已穿過止損價但倉位還在（交易所條件單失靈）
# 就主動市價平倉兜底。1=開（建議）0=關
SL_FAILSAFE=1
# 止盈保險絲（合約）：對帳時若價格已達某段止盈但該段沒成交，就逐段市價補平那一段。1=開 0=關
TP_FAILSAFE=1
# 每筆固定投入多少 USDT（限價進場用此金額換算數量）
TRADE_USDT=50
# 自動最小金額：1=忽略上面的 TRADE_USDT，改成每筆依該交易對「能成功下單＋拆得出分批」
# 的最小金額自動計算並下單（適合想用最小資金多開幾筆）；0=用固定 TRADE_USDT
AUTO_MIN_AMOUNT=0
# AUTO_MIN_AMOUNT=1 時：名目 = 最小可下量 × MIN_AMOUNT_MULT × LEVERAGE。
# 最小量 × 此倍數 = 實際投入的保證金本金（再乘槓桿才是下單名目）。
MIN_AMOUNT_MULT=10
# 合約固定本金（USDT，建議用這個）：>0 時改用「名目 = 本金 × 槓桿」算倉位，每筆保證金一致。
# 設了它就會忽略上面的 TRADE_USDT / AUTO_MIN_AMOUNT 名目計算。0=沿用上面兩者。
MARGIN_USDT=0
# 最多同時持有幾筆未結倉（上限保護，避免訊號爆量時無限下單）
MAX_OPEN_TRADES=5
# 四個目標價的分批平倉比例（逗號分隔，會自動正規化）；倉位太小會自動降段、近端優先
TP_RATIOS=30,30,20,20
# 合約止損：SL1 = min(訊號SL1距離, 上限 SL1_PCT%)；SL2 = SL1 × SL2_MULT。
# 例：訊號SL1 -3% → 上軌 -3%/下軌 -6%；訊號SL1 -8%（超上限）→ 上軌 -5%/下軌 -10%。
# SL2_MULT=0 → 捨棄 SL2，改單一止損守全倉。每中一段 TP 都會逐段往上爬（保本→TP1→…）。
SL1_PCT=5
SL2_MULT=2
# 止損腿（stop-limit）的限價，設在觸發價下方多少 %，確保急殺時掛得掉
SL_LIMIT_BUFFER_PCT=0.3
# 賣單預留手續費緩衝（%）：拆單時從買進量扣掉，避免賣超實得數量
SELL_FEE_BUFFER_PCT=0.1
# 限價買單未成交時，輪詢檢查的間隔秒數
ENTRY_POLL_INTERVAL=5
# 保險絲對帳間隔（秒）：即時反應已交給 WebSocket，這只當慢速兜底（漏訊息/斷線/重啟回復）
TRADE_MONITOR_INTERVAL=120
# 動態止損總開關：1=每段 TP 成交後把雙軌止損逐段上移（階梯鎖利）；0=止損固定在初始 SL1/SL2
BREAKEVEN_AFTER_TP1=1
# 淨保本手續費%：階梯爬到「進場價」那階時，止損設在 進場 ×（1±此%）涵蓋來回手續費，被掃也不虧
BREAKEVEN_FEE_PCT=0.1
# 下單止損倍數：初始上軌 SL1 = 進場 + 此倍數 ×（訊號止損1 − 進場），把止損1 距離放大這麼多倍
# 例：訊號 SL1 = -5%、倍數 2 → 初始上軌掛在 -10%。轉發訊息仍顯示訊號原始止損，不受影響
SL_MULTIPLIER=2
# 限價買單超過幾分鐘未成交就撤單、釋放持倉額度（0=永不超時，一直等成交）
# 部分成交才超時的話，會保護已成交部分、只撤掉未成交的剩餘
ENTRY_TIMEOUT_MIN=30
