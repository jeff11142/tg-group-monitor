# 機密：API 憑證與登入 session 絕不可進版控
.env
*.session
*.session-journal

# 記錄檔
*.jsonl
messages.jsonl

# 接收者 DB（包含他人 chat_id，等同個資；應該保留在 VPS 上而不入版控）
*.db

# Python
__pycache__/
*.pyc
.venv/
venv/

# 回測/分析的歷史資料與產出（大檔、可由腳本重新產生，不入版控）
btc_1h.json
dca_data.json
dca_series.json
macd_grid.json

# 訊號樣本暫存
signal*.txt

# macOS
.DS_Store
